/**
 * Learning Hub Layout — Simple wrapper (tabs are handled by LearningHubClient)
 */

export default function LearningLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
